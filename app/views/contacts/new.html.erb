<h1>Add a Contact</h1>

<%= form_for @contact do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :phone %>
	<%= f.text_field :phone %>
	<%= f.hidden_field :user_id, value: current_user.id %>
	<%= f.submit %>
<% end %>